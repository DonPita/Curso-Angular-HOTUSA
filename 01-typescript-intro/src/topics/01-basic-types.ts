//Los dos puntos para tener a primera vista de que tipo es la variable
let name: string = 'Strider';

//Indicar que la variable hpPoints puede ser numero o el string Full
let hpPoints: number | 'Full' = 100;
hpPoints = 'Full';

const isAlive: boolean = true;

console.log({
    name, hpPoints, isAlive
});










export{};